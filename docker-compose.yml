services:
  db:
    image: postgres:16
    container_name: bventy_postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  adminer:
    image: adminer
    container_name: bventy_adminer
    restart: always
    ports:
      - "8081:8080"

volumes:
  pgdata:
